query CurrentUser {
  currentUser {
    id
    label
  }
}

query AppInfo {
  appInfo {
    version
  }
}

mutation Login($input: LoginInput!) {
  login(input: $input) {
    id
    label
  }
}

mutation Logout {
  logout
}
